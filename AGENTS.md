# 项目执行规则

本项目把文件系统视为事实来源。开始工作前依次读取 `PROJECT.md`、`ROADMAP.md`、`STATE.md`、`TASKS.md`、`REVIEW.md` 和 `RUN_LOG.md`。

## 权限与安全

- 只操作用户本人明确授权的小红书账号。
- 默认只读盘点。删除发布内容、取消收藏、删除收藏夹前，必须生成最新版动作计划并取得明确确认。
- 计划变化后，旧确认失效；保护名单中的目标不得进入删除队列。
- 使用独立持久化浏览器配置，不默认接管用户日常 Chrome。
- 登录、验证码和人机验证由用户完成；不得收集、输出或提交 Cookie、Token、二维码、验证码及浏览器 Profile。
- 不调用、公开或尝试绕过小红书私有签名和风控机制。公开版使用正常页面交互。
- 出现登录页、游客身份、验证码、HTTP 406/429/461、错误码 300011 或连续两次状态不一致时，立即停止写操作并保存检查点。
- 不把接口成功、脚本完成、本地测试通过误报为账号最终状态；最终状态必须重新读取页面验证。

## 项目状态

- `PROJECT.md`：目标、范围、排除项和成功标准。
- `ROADMAP.md`：里程碑和验收门槛。
- `STATE.md`：当前唯一任务、阻碍和下一动作。
- `TASKS.md` 与 `tasks/`：任务队列和详细定义。
- `REVIEW.md`：风险、偏差和待确认事项。
- `RUN_LOG.md`：按时间追加的简短执行记录。
- `MILESTONES.md`：已完成里程碑索引。

## 开发规则

- 第一版目标环境是 Windows、Codex 和 `agent-browser`。
- 两个 Skill 必须可独立安装和理解，不依赖本机专有路径。
- 示例只能使用虚构账号、收藏夹和笔记 ID。
- 运行数据写入 `.xhs-organizer/`，并由 `.gitignore` 排除。
- 使用 Node.js 内置模块完成离线校验，避免无必要依赖。
- 修改 Skill 后运行 `quick_validate.py`；修改脚本后运行 `npm test` 与 `npm run check`。
